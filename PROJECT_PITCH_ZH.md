# Project Pitch

## 标题

`Mayuri-Amadeus`: 从单个角色语音模型，到可复现的角色语音系统

## 核心主张

这个项目不是在单纯训练一个椎名真由理语音模型，而是在构建一套可复现、可扩展、可实验的角色语音系统方法。

它的出发点很简单：

- 角色语音不应该只是一个权重文件
- 角色语音需要数据、参考音频、配置和推理策略一起工作
- 角色语音真正有趣的地方，不在训练结束，而在训练之后如何与 LLM、检索和风格控制模块结合

## 问题背景

现有很多角色语音项目可以产出“像某个角色的声音”，但很难做到下面几点：

- 让别人复现整个流程
- 让同一套流程迁移到新角色
- 系统性管理参考音频与情绪控制
- 把训练结果接入 LLM 或对话系统
- 做出有研究味、但不需要过度正式的实验 playground

这意味着很多项目停留在“展示一个结果”，而没有形成“一个方法”。

## 这个项目要做什么

这个项目打算把角色语音拆成两个协同部分：

### 一部分是通用训练框架

它负责：

- 数据集目录规范
- manifest 构建与过滤
- prepare、训练、导出、试听
- 参考音频库与角色 profile 组织
- 实验脚本和可复现工作流

### 一部分是具体角色实例

以 `Mayuri-Amadeus` 为例，它负责：

- 真由理语音数据
- 对应文本标注
- 数据过滤规则
- 参考音频库
- 训练结果与试听案例
- 围绕该角色展开的 demo 和 playground

这样做的意义是：

- 框架提供抽象能力
- 角色仓库提供具体实践

## 技术路线为什么有意思

当前采用的是 GPT-SoVITS 这一类 reference-driven TTS 路线。

它的优势不在于“最前沿”，而在于它很适合角色语音这个问题：

- 训练成本相对可控
- 对角色数据集较友好
- 参考音频驱动的控制方式很自然
- 很适合和 LLM 拼接成有趣系统

在这个系统里：

- GPT 决定“怎么说”
- SoVITS 决定“像谁说”
- 参考音频决定“这次按什么状态说”

这让角色语音从“固定音色播报器”变成了“可控表达系统”。

## 参考音频为什么是亮点而不是包袱

很多人会把参考音频理解成一个额外负担，但对角色语音来说，它其实是一个很有价值的控制接口。

同一个角色也会有多种表达状态：

- 日常
- 元气
- 担心
- 柔和
- 紧张
- 悲伤

如果把参考音频当作“推理时的表演提示”，那么这个项目就不只是一个 TTS，而更像一个：

`reference-conditioned character speech system`

这也让它天然适合和检索、LLM、情绪规划结合。

## 为什么它有社区价值

这个项目的价值不应该只靠“真由理模型好不好听”来衡量。

它真正能贡献给社区的是：

- 角色语音训练流程的标准化
- 角色数据集组织方式
- 参考音频库的工程化管理
- 从训练到推理的可复现链路
- 与 LLM 联动的角色语音 playground 基础设施

也就是说，它贡献的不是一个孤立结果，而是一种可迁移的方法。

## 为什么它有研究味

这个项目不追求论文式大而全，但它天然包含几个有研究趣味的问题：

- 参考音频检索是否能提升角色一致性
- 不同 epoch 对音色和自然度的影响如何比较
- LLM 风格改写是否增强角色沉浸感
- “说什么”和“怎么说”能否被更清晰地解耦
- 角色一致性能否被自动评测

这些问题都可以做成很轻量、但很有意思的实验。

## 可以发展出的 playground

### 1. 角色对话机

LLM 负责内容生成，角色模块负责风格改写，TTS 负责最终配音。

### 2. 情绪参考检索器

根据目标文本自动选择更合适的参考音频，形成 inference-time 风格控制。

### 3. 角色一致性评测台

系统比较不同模型版本、不同参考音频和不同推理参数。

### 4. Character Speech Agent

把角色语音封装成一个有记忆、有风格偏好、有语气控制的 speech agent。

## 最适合的项目叙述方式

如果要向别人介绍这个项目，最好的说法不是：

> 我训练了一个真由理语音模型。

而是：

> 我在构建一个 reference-driven 的角色语音系统，并用真由理作为第一个完整角色实例。这个系统不仅关注音色复现，也关注参考音频管理、角色风格控制，以及它如何与 LLM 和轻量实验平台结合。

## 一句话电梯版

> `Mayuri-Amadeus` 是一个把角色语音训练、参考音频控制和 LLM 风格联动结合起来的角色语音实验项目；它的目标不是产出一个孤立模型，而是沉淀一套可复现、可扩展、可迁移的角色语音方法。
