# Project Overview

这是一组围绕“二次元角色语音系统”展开的项目，而不是一个单独的模型训练脚本。

它的目标不是只做出一个听起来像角色的声音，而是把角色语音拆成可复现、可扩展、可实验的几个部分：

- 数据集整理与过滤
- 角色语音训练
- 参考音频驱动的风格控制
- 与 LLM 联动的角色表达
- 面向社区复用的通用工作流

## 一句话简介

这是一个把 reference-driven TTS、角色数据集和 LLM 风格控制结合起来的角色语音实验平台；`Mayuri-Amadeus` 是它的第一个完整角色实例。

## 项目想解决什么问题

很多角色语音项目都能展示一个结果，但很难回答下面这些问题：

- 这套流程别人能不能复现
- 换一个角色后能不能直接复用
- 推理时的情绪和语气如何控制
- 模型权重之外还需要哪些资产
- 训练完成后如何把它接进 LLM、对话和 playground

这个项目的核心想法是：角色语音不应该只是一个权重文件，而应该是一套完整系统：

`weights + refs + profile + inference scripts`

## 为什么仍然使用 GPT-SoVITS

GPT-SoVITS 不是最前沿的语音生成路线，但它依然非常适合角色语音项目：

- 对较小规模角色数据集友好
- 工程门槛和训练成本相对可控
- 参考音频驱动的控制方式直观且可玩
- 容易与 LLM、检索、规则系统结合

在这个项目里，我们不把它当作“最终答案”，而把它当作一个很适合构建角色语音系统的基础设施。

## 为什么是两个仓库

这个项目天然适合拆成两个层次。

### 1. 通用角色语音训练框架

这个仓库负责方法，而不是某一个角色本身。它应该提供：

- 数据集目录规范
- manifest 构建与过滤
- `prepare / train-sovits / train-gpt / export / preview`
- 参考音频库组织方式
- 角色 profile 配置
- 实验日志与评测脚本

它的目标是让“换一个角色重新训练”变成一件低摩擦的事。

### 2. `Mayuri-Amadeus`

这个仓库是真由理的角色实例。它负责：

- 真由理语音数据
- 转写与过滤规则
- 参考音频库
- 训练配置
- 权重与试听结果
- 围绕真由理展开的 demo 与 playground

也就是说：

- 通用框架解决“怎么做”
- `Mayuri-Amadeus` 回答“真由理这件事具体是什么样子”

## 两个模型分别做什么

当前方案里有两个核心模型。

### GPT

GPT 负责更高层的表达规划，主要影响：

- 节奏
- 停顿
- 语气
- 情绪走势
- 长句自然度

它更接近在决定“这句话怎么说”。

### SoVITS

SoVITS 负责把高层表示真正解码成音频，主要影响：

- 音色像不像角色
- 声音稳定性
- 发音清晰度
- 质感和保真度

它更接近在决定“这句话像谁说出来”。

## 为什么参考音频是系统的一部分

在这条技术路线里，模型权重并不是唯一资产。

参考音频在推理时承担两件事：

- 给 GPT 一个“这次按什么状态说”的提示
- 给 SoVITS 一个“这次按什么质感发声”的锚点

因此，一个完整角色语音系统除了模型权重，还应该交付：

- 默认参考音频
- 情绪参考音频库
- 对应参考文本
- 推理默认参数
- 角色 profile

## 这个项目对社区的价值

它的社区价值不在于“又多了一个角色模型”，而在于下面几件更可复用的事：

- 把角色语音训练流程标准化
- 把参考音频管理变成正式设计对象
- 提供可复现的角色语音工作流
- 让新角色数据集可以即插即用
- 为 LLM + 角色语音 playground 提供基础设施

## 训练完成后还能做什么

这个项目最终不应该停在“生成一段声音”，而应该长成几个可玩的方向：

- 角色对话机：LLM 生成内容，角色模块改写风格，TTS 输出角色语音
- 情绪参考检索器：根据文本自动挑选参考音频
- 角色一致性评测台：比较不同 epoch、不同 reference、不同推理参数
- ADV / Galgame 台词机：让 LLM 场景对话直接转成角色配音
- Character Speech Agent：把角色模型包装成一个有风格的 speech agent

## 近期路线图

### 第一阶段

- 完成真由理 SoVITS 和 GPT 训练
- 整理参考音频库
- 固化试听与推理流程

### 第二阶段

- 从 `Mayuri-Amadeus` 中抽出通用框架
- 统一角色 profile、refs、weights、manifests 规范
- 支持新角色数据集即插即用

### 第三阶段

- 增加 LLM 驱动的角色对话 playground
- 增加情绪参考检索实验
- 增加角色一致性评测工具

## 适合放在首页的项目主张

如果要用一段话概括这个项目，可以这样说：

> 这不是一个孤立的角色语音模型，而是一套面向角色数据集、参考音频和 LLM 风格控制的角色语音系统。`Mayuri-Amadeus` 是它的第一个完整角色实例，目标是把角色语音从一次性训练结果，整理成可复现、可扩展、可实验的开源资产。
