# Project Narrative

## 项目定位

这个项目不只是“训练一个真由理语音模型”，而是试图把二次元角色语音这件事拆成两个可以长期演化的方向：

1. 一个通用的角色语音训练与推理框架
2. 一个围绕椎名真由理展开的角色实例仓库

前者解决“如何稳定复现一个角色语音系统”，后者回答“当这个框架落在一个具体角色上时，数据、风格、参考音频、推理策略应该如何组织”。

从工程角度看，这是一个角色语音工作流项目；从研究趣味看，它是一个 reference-driven character speech playground；从社区角度看，它希望提供的不只是模型结果，而是一套可移植、可复现、可扩展的角色语音方法。

## 为什么做这件事

现有的很多角色语音项目，往往停留在下面几种状态：

- 只有一个训练好的权重，没有数据组织方法
- 有数据和脚本，但流程强依赖作者本地环境
- 能推理，但缺少参考音频管理和角色风格控制
- 能生成声音，但很难与 LLM、角色对话、实验评测结合

这导致一个常见问题：别人也许能“听到一个结果”，但很难真正复现整个过程，更难在此基础上扩展新角色、新玩法和新实验。

这个项目想解决的不是“再训练一个单独的角色声音”，而是把“角色语音系统”从一次性成果，整理成一种可以复用的资产。

## 核心问题定义

我们关心的问题可以分成三层：

### 1. 角色音色复现

如何利用一个特定角色的有限语音片段，训练出一个足够像该角色的声音系统。

### 2. 角色表达控制

如何让模型不仅“像这个角色”，还能够在参考音频的帮助下表现出不同的说话状态，例如：

- 平静
- 元气
- 担心
- 低落
- 撒娇

### 3. 角色系统化使用

如何把一个角色语音模型，从“一个能生成音频的模型”，提升为“一个可以和 LLM、对话系统、实验评测、playground 一起工作的角色代理组件”。

## 为什么还采用 GPT-SoVITS 这一类方案

从严格意义上说，GPT-SoVITS 并不是最前沿的语音生成范式。它不是单一大模型、不是端到端统一代理，也不是当前最强调细粒度可控性的研究路线。

但它依然有明显价值：

- 对少量角色数据较友好
- 训练和推理成本相对可控
- reference-based 的风格迁移直观且可玩
- 适合作为角色语音工程的基础设施
- 很容易与 LLM、检索、规则系统拼接成更有趣的系统

因此，这个项目的目标并不是证明“GPT-SoVITS 是最先进的”，而是回答：

> 在一个可用、可复现、可扩展的工程框架下，如何把 reference-driven TTS 变成一个真正可玩的角色语音系统。

## 两个项目的拆分思路

### 项目一：通用角色语音训练框架

这个仓库的职责不是承载某一个角色，而是承载方法。

它应该提供：

- 统一的数据集目录规范
- manifest 构建与过滤工具
- `prepare / train-sovits / train-gpt / export / preview` 工作流
- 参考音频库的组织规范
- 角色 profile 配置
- 实验日志、评测脚本、导出脚本
- 与上游训练框架的解耦封装

它的目标用户是：

- 想训练其他角色的人
- 想复现流程的人
- 想在角色语音上做实验的人

可以把它理解成一个 `character-voice-lab`。

### 项目二：`Mayuri-Amadeus`

这个仓库是通用框架上的一个具体角色实例。

它应该承载：

- 真由理的训练数据
- 真由理的文本标注
- 真由理的过滤规则
- 真由理的参考音频库
- 真由理的训练配置
- 真由理的模型权重与试听结果
- 围绕真由理角色特征展开的 demo 和 playground

它的目标不是抽象，而是角色化。

也就是说：

- 通用框架解决“怎么做”
- `Mayuri-Amadeus` 回答“真由理这件事具体是什么样子”

## 技术叙事：两个模型分别做什么

当前方案由两个核心模型组成：

### GPT

GPT 负责把文本和参考语音的上下文，转换成更高层的语义和韵律表示。

它更接近在决定：

- 这句话怎么说
- 停顿在哪里
- 节奏如何展开
- 语气更平静还是更活泼
- 长句如何保持自然

### SoVITS

SoVITS 负责把这些高层表示真正解码成音频。

它更接近在决定：

- 这句话听起来像不像真由理
- 声音质感是否稳定
- 发音是否清楚
- 音色细节是否保真

因此，这套系统不是“一个权重解决所有事”，而是：

- GPT 决定“怎么说”
- SoVITS 决定“像谁说”

这也是为什么项目后续很适合和 LLM 结合。LLM 可以负责内容和风格规划，语音模型负责把这个规划转成具体发声。

## 参考音频为什么重要

这个项目的一个核心思想是：角色语音不是一个固定点，而是一整个风格分布。

同一个角色也会有不同状态：

- 日常语气
- 元气状态
- 紧张状态
- 柔和状态
- 情绪波动时的说法

参考音频在推理阶段的作用，不只是提供音色，而是在告诉模型：

> 这一次，请按这种状态来表达。

因此，最终可交付物不应只有模型权重，还应包括：

- 默认参考音频
- 多情绪参考音频库
- 参考文本
- 角色 profile
- 推荐推理参数

换句话说，一个完整角色语音系统应该是：

`weights + refs + profile + inference scripts`

而不是只有一个 `.pth` 或 `.ckpt` 文件。

## 社区贡献应该落在哪里

如果这个项目只提供“真由理的一个训练结果”，社区价值是有限的。

但如果它提供的是下面这些东西，贡献会更扎实：

### 1. 可复现的角色语音工作流

把一套原本依赖个人经验和本地环境的流程，整理成：

- 可以被别人 clone 后运行
- 可以替换数据集后复用
- 可以持续迭代的训练框架

### 2. 角色数据的组织规范

例如：

- 原始音频怎么放
- 文本怎么标注
- manifest 怎么生成
- 过滤规则如何表达
- 情绪参考库如何构建

### 3. 参考音频驱动的角色控制实践

很多人知道 reference-based TTS 能做风格迁移，但缺少一个面向角色语音的、可落地的组织方式。

这个项目可以把“参考音频”从临时输入，升级成一个正式设计对象。

### 4. 轻量但有研究味的实验平台

这个项目不必追求论文级 SOTA，也仍然可以有研究价值，例如：

- 不同参考音频对角色一致性的影响
- 不同 epoch 对音色和自然度的影响
- LLM 角色改写是否提升角色沉浸感
- 情绪参考检索是否比手动选择更稳定

## 可以延展出的 playground

项目最有趣的部分，不是训练本身，而是训练完成后如何把角色声音“变成一个系统”。

下面这些 playground 都有明显的扩展空间。

### 1. 角色对话机

流程：

- LLM 生成回答
- 角色风格模块把回答改写成“真由理会怎么说”
- 情绪检索模块挑选一条合适的参考音频
- GPT-SoVITS 生成最终语音

这个系统可以研究：

- 语义正确性与角色一致性的平衡
- 文本风格改写对角色感的影响
- 参考音频检索对表现力的影响

### 2. 情绪参考检索器

为参考音频建立轻量标签：

- `neutral`
- `happy`
- `gentle`
- `worried`
- `sad`
- `excited`

然后根据目标文本，让一个小型 LLM 或规则系统自动选择参考音频。

这本质上是在做：

> inference-time style control by reference retrieval

非常适合做成一个有科研味的小实验。

### 3. 角色一致性评测台

对同一句文本，固定或改变以下变量：

- SoVITS epoch
- GPT epoch
- reference audio
- temperature / top_p

然后比较：

- 角色相似度
- ASR 回译正确率
- 主观自然度
- 情绪稳定性

这个 playground 很适合把“训练结果”转成“实验对象”。

### 4. ADV / Galgame 台词机

让 LLM 生成场景对话，再由角色语音系统朗读。

可扩展方向包括：

- 剧情上下文记忆
- 场景情绪变化
- 多角色切换
- 长对话中的角色一致性

### 5. Character Speech Agent

把角色模型包装成一个 speech agent：

- 有人格设定
- 有参考音频记忆库
- 有语气选择机制
- 能把 LLM 输出转成“像角色本人在说话”的语音

这个方向虽然不追求正式论文路线，但非常有研究原型的味道。

## 一个更完整的项目主张

这个项目最终可以被概括为：

> 我们不是在训练一个单独的角色声音，而是在构建一个 reference-driven、可复现、可扩展的角色语音系统，并探索它如何与 LLM、检索和风格控制模块结合，变成一个有表现力的角色代理。

这句话同时覆盖了：

- 工程目标
- 研究趣味
- 社区贡献
- playground 空间

## 近期路线图

### 第一阶段：把真由理项目做扎实

- 完成 SoVITS 与 GPT 训练
- 整理真由理参考音频库
- 固化默认推理与试听流程
- 形成可复用的角色资产包

### 第二阶段：抽象出通用框架

- 从 `Mayuri-Amadeus` 中抽出通用脚本
- 统一 profile、refs、weights、manifests 规范
- 让新角色数据集可以即插即用

### 第三阶段：补上 playground 与实验台

- 角色对话 demo
- 情绪参考检索 demo
- epoch / reference 对比评测
- LLM 驱动的角色语音生成 demo

## 一句话版本

如果要把整个项目讲成一句话，可以这样说：

> 这是一个把 reference-driven TTS、角色数据集和 LLM 风格控制结合起来的角色语音实验平台；`Mayuri-Amadeus` 是它的第一个完整角色实例，而不是唯一目标。
